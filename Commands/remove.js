const name = 'remove'
const execute = (params = {}) => {
  switch (params.args[1].toLowerCase()) {
    case 'permissions':
      break
    case 'bypasses':
      break
    default:
      break
  }
}

export { name, execute }
